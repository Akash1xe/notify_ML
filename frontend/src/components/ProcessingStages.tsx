import { STAGE_LABELS, UI_STAGES, type UiStage } from '../lib/stages';
export function ProcessingStages({active,failed=false}:{active:UiStage;failed?:boolean}){const activeIndex=UI_STAGES.indexOf(active);return <ol className="stage-list">{UI_STAGES.slice(0,-1).map((stage,i)=>{const state=i<activeIndex?'done':i===activeIndex?(failed?'failed':'active'):'pending';return <li key={stage} className={`stage ${state}`}><span aria-hidden="true" className="stage-icon">{state==='done'?'✓':state==='active'?'●':state==='failed'?'!':'○'}</span><span>{STAGE_LABELS[stage]}</span></li>})}</ol>}
