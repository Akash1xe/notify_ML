import { describe, expect, it } from 'vitest'; import { mapBackendStageToUiStage } from './stages';
describe('stage mapping',()=>{it('maps all phase boundaries',()=>{expect(mapBackendStageToUiStage('DOWNLOADING')).toBe('VIDEO_PREPARATION');expect(mapBackendStageToUiStage('SAMPLING_FRAMES')).toBe('VISUAL_ANALYSIS');expect(mapBackendStageToUiStage('GENERATING_CANDIDATES')).toBe('CANDIDATE_SELECTION');expect(mapBackendStageToUiStage('TRANSCRIBING_AUDIO')).toBe('TRANSCRIPTION');expect(mapBackendStageToUiStage('ANALYZING_VISUAL_CANDIDATES')).toBe('SEMANTIC_ANALYSIS');expect(mapBackendStageToUiStage('DETECTING_SCREENSHOT_DUPLICATES')).toBe('SCREENSHOT_SELECTION');expect(mapBackendStageToUiStage('PLANNING_DOCUMENT_LAYOUT')).toBe('DOCUMENT_PREPARATION');expect(mapBackendStageToUiStage('GENERATING_PDF')).toBe('PDF_GENERATION')});it('falls back safely',()=>expect(mapBackendStageToUiStage('FUTURE_STAGE')).toBe('VIDEO_PREPARATION'))});
